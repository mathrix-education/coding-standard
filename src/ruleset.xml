<?xml version="1.0"?>
<!-- PHP_CodeSniffer global configuration file -->
<ruleset
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        name="Mathrix Eucation API"
        xsi:noNamespaceSchemaLocation="../vendor/squizlabs/php_codesniffer/phpcs.xsd"
>
    <description>The coding standard of Mathrix Education API.</description>

    <!-- Rules inherited from Doctrine -->
    <rule ref="Doctrine">
        <exclude name="Generic.Formatting.SpaceAfterNot"/>
        <exclude name="Generic.Formatting.SpaceAfterCast"/>
        <exclude name="Squiz.Strings.DoubleQuoteUsage.ContainsVar"/>
        <exclude name="SlevomatCodingStandard.Exceptions.ReferenceThrowableOnly"/>
        <exclude name="SlevomatCodingStandard.TypeHints.TypeHintDeclaration"/>
    </rule>

    <!-- Force no whitespace after a type cast -->
    <rule ref="Generic.Formatting.NoSpaceAfterCast"/>

    <!-- Require no space around colon in return types -->
    <rule ref="SlevomatCodingStandard.TypeHints.ReturnTypeHintSpacing">
        <properties>
            <property name="spacesCountBeforeColon" value="0"/>
        </properties>
    </rule>

    <!-- Do not require a vendor namespace for migrations and DatabaseSeeder -->
    <rule ref="PSR1.Classes.ClassDeclaration.MissingNamespace">
        <exclude-pattern>database/migrations</exclude-pattern>
        <exclude-pattern>database/seeds/DatabaseSeeder.php</exclude-pattern>
    </rule>

    <!-- Do not require a class to match filename for migrations -->
    <rule ref="Squiz.Classes.ClassFileName.NoMatch">
        <exclude-pattern>database/migrations</exclude-pattern>
    </rule>
</ruleset>
